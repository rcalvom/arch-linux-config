# Qtile

This folder contains the configuration for the Qtile window manager.

- `config/` stores the main config (`config.py`) plus settings and theme data.
- `wallpapers/` holds wallpapers used by the Qtile setup.
