# Charge when below this level after a threshold has stopped charging.
CHARGE_START_THRESHOLD=0

# Apply this ceiling when AC power and a USB device whose product contains the
# configured pattern are both present.
DOCK_CHARGE_END_THRESHOLD=50
DOCK_PRODUCT_PATTERN=dock

# Apply this ceiling for direct AC power and battery operation.
AC_CHARGE_END_THRESHOLD=90
