#!/usr/bin/env bash
set -euo pipefail

STATE_DIR="${XDG_STATE_HOME:-$HOME/.local/state}/arch-linux-config"
install -dm700 "$STATE_DIR"

if [[ $# -eq 1 && "$1" == Hyprland ]]; then
  set -- /usr/bin/start-hyprland
fi

exec "$@" >> "$STATE_DIR/wayland-session.log" 2>&1
