#!/usr/bin/env bash
set -euo pipefail

STATE_DIR="${XDG_STATE_HOME:-$HOME/.local/state}/arch-linux-config"
install -dm700 "$STATE_DIR"

exec /usr/bin/startx /usr/bin/env "$@" >> "$STATE_DIR/xsession.log" 2>&1
