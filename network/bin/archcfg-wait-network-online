#!/usr/bin/env bash
set -euo pipefail

for _ in {1..60}; do
  if /usr/bin/ip -4 route show default | /usr/bin/grep -q '^default ' &&
    /usr/bin/resolvectl dns | /usr/bin/grep -q '^Link '; then
    exit 0
  fi

  /usr/bin/sleep 1
done

exit 1
